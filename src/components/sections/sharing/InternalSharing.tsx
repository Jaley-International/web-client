import {Node} from "../../../helper/processes";

interface Props {
    node: Node;
    closeCallback: () => void;
}

function InternalSharing(props: Props): JSX.Element {
    return (
        <>
            Internal sharing : Work in progress.
        </>
    );
}

export default InternalSharing;
